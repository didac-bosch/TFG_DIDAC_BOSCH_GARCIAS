# ============================================================================
# Estación de tierra EZDrone — dependencias (portátil Windows + RTX 5060)
# ============================================================================
# Instalar en un venv con Python 3.12 (NO 3.14: el stack GPU no tiene wheels
# fiables para 3.14). Ver SETUP_WINDOWS_GPU.md.
#
#   py -3.12 -m venv .venv
#   .venv\Scripts\activate
#   pip install -r requirements.txt
#
# IMPORTANTE: torch/torchvision se instalan desde el índice cu128 (Blackwell,
# sm_120). El --extra-index-url de abajo lo cubre. Si pip coge una rueda CPU,
# instálalas a mano antes:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu128
# ----------------------------------------------------------------------------
--extra-index-url https://download.pytorch.org/whl/cu128

# Visión / GPU
torch
torchvision
ultralytics            # YOLO11-pose (.pt) + tracker BoT-SORT (re-ID)
opencv-python
numpy

# WebRTC (vídeo + datachannels) y señalización
aiortc
av
websockets

# MQTT (comandos / confirmaciones de estado)
paho-mqtt<2            # el codigo usa la API de callbacks 1.x (mqtt.Client(name),
#                      # on_connect(client,userdata,flags,rc)). paho 2.x la rompe.

# Calibración de cámara
pyyaml

# DJI Tello (follow/orbit)
djitellopy>=2.5
pygame                 # TelloLink: lectura del mando/joystick (tello_joystick)

# ArduPilot / Pixhawk / SITL (dronLink)
pymavlink
pyserial             # pymavlink lo usa para abrir el puerto COM (radio de telemetría)
# dronekit              # NO se usa: el proyecto vuela con dronLink/ (carpeta local).
#                       # Dejar comentado evita arrastrar 'future'/'past' (rompe en 3.12).
